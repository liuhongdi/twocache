<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.twocache.demo.mapper.GoodsMapper">

	<select id="selectOneGoods" parameterType="long" resultType="com.twocache.demo.pojo.Goods">
        select * from goods where goodsId=#{goodsId}
    </select>

</mapper>